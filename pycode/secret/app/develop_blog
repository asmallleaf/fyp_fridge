2019/2/22
# 做完了三个路由 additem,deleteitem,getlist
# 数据库模型建立完毕
# 完善了一下toolbox，这个类的意义终于找到了，目前朝着便于项目移植方向发展
  但是其中的VerifyError类有点不太好
# 把toJson函数改到toolbox了，以后应该有用吧，所以服务端的返回json完成了
# 大体框架都完成了，接下来完成客户端json对接以后就可以开始user类的开发了
# 由于数据库代码有改过reference关系，所以模型记得检查，没改完

2019/2/26
# 突然发现必须先完成后端上架服务器了，所以开始制作验证层和用户注册登陆
  目前完成了注册的基本逻辑，但是还没有返回token
# 优化了verify error 这个工具类的结构了，现在的更加合理，只不过需要重写很多东西
# 增加了tohash等工具函数或者类，验证层方面启用了工厂模式，不过现在只要验证用户还好
# toolbox的理解也加深了，实际最好再建一个supportTool用来专门应用tool函数，但是因为有验证层就算了
# 发现了路由函数的结构还需优化，现在两个模式post和get明明用一个逻辑但是不太好，后面改起来不太方便
# 验证层现在完成了注册验证，登陆验证还需思考如何分离数据库和验证层，便于以后迁移验证层

2019/2/27
# 啊，今天完成的不多呢，发现了blueprint的机构还是有问题，还是要想办法优化
  目前的蓝天实际是多个蓝图，是不对的
# 改动了login的逻辑，还是采用了supportTool来完成分离
# 还需要重新构建webapi下的认证功能，但之前仍需测试

2019/3/14
# 增加了最后的附加功能，不过没有写Logout呢，因为不想做token过期了
# 甚至优化了一下蓝图框架并且规范了一些写法